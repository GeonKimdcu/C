#include <stdio.h>
#include <math.h>
intpow(int m, int n);

int main(void) {
	int x, y;
	printf("x의 값 :");
	scanf_s("%d", &x);
	printf("y의 값 :");
	scanf_s("%d", &y);
	return 0;
}

int intpow(int m, int n) {
	return mn;
}